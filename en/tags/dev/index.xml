<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title>Dev - Tag - Chaos Talk</title><link>https://chaoruan.xyz/en/tags/dev/</link><description>Dev - Tag - Chaos Talk</description><generator>Hugo -- gohugo.io</generator><language>en</language><managingEditor>blog@chaoruan.dev (Chao Ruan)</managingEditor><webMaster>blog@chaoruan.dev (Chao Ruan)</webMaster><lastBuildDate>Sun, 29 Mar 2026 17:00:00 +1100</lastBuildDate><atom:link href="https://chaoruan.xyz/en/tags/dev/" rel="self" type="application/rss+xml"/><item><title>How I set up every machine</title><link>https://chaoruan.xyz/en/posts/dev-kit-philosophy/</link><pubDate>Sun, 29 Mar 2026 17:00:00 +1100</pubDate><author>blog@chaoruan.dev (Chao Ruan)</author><guid>https://chaoruan.xyz/en/posts/dev-kit-philosophy/</guid><description><![CDATA[<p>I keep the same tools on every machine. MacBook, Windows gaming PC, Surface for work. One command and I&rsquo;m done. <a href="https://www.chezmoi.io/" target="_blank" rel="noopener noreffer ">chezmoi</a> templates handle what differs between platforms, and I don&rsquo;t think about it again. The point is to sit down anywhere and have my hands already know what to do.</p>
<p>Part of making this work is choosing tools that run everywhere. <a href="https://neovim.io/" target="_blank" rel="noopener noreffer ">Neovim</a>, <a href="https://github.com/junegunn/fzf" target="_blank" rel="noopener noreffer ">fzf</a>, <a href="https://github.com/BurntSushi/ripgrep" target="_blank" rel="noopener noreffer ">ripgrep</a>, <a href="https://github.com/sharkdp/bat" target="_blank" rel="noopener noreffer ">bat</a>, <a href="https://github.com/jesseduffield/lazygit" target="_blank" rel="noopener noreffer ">lazygit</a>, <a href="https://github.com/sxyazi/yazi" target="_blank" rel="noopener noreffer ">yazi</a>, <a href="https://starship.rs/" target="_blank" rel="noopener noreffer ">Starship</a>, <a href="https://mise.jdx.dev/" target="_blank" rel="noopener noreffer ">mise</a>. They all work on macOS, Linux, and Windows. Where that&rsquo;s not possible (Zsh vs PowerShell, Karabiner vs Kanata), the config is structured so the experience still feels the same. The fewer platform-specific exceptions, the less I have to remember.</p>
<h2 id="the-terminal-is-the-workspace">The terminal is the workspace</h2>
<p>Neovim edits code, fzf finds things, lazygit handles version control, <a href="https://github.com/tmux/tmux" target="_blank" rel="noopener noreffer ">tmux</a> keeps sessions alive, yazi moves files around. They&rsquo;re separate programs, but they share conventions that make them feel like one. Pane navigation works the same across tmux and Neovim via <a href="https://github.com/christoomey/vim-tmux-navigator" target="_blank" rel="noopener noreffer ">vim-tmux-navigator</a>. The same leader-key groups work in standalone Neovim and inside VS Code. One set of keybindings in my head, regardless of context.</p>
<p>When I have to use <a href="https://code.visualstudio.com/" target="_blank" rel="noopener noreffer ">VS Code</a>, the <a href="https://marketplace.visualstudio.com/items?itemName=asvetliakov.vscode-neovim" target="_blank" rel="noopener noreffer ">VSCode Neovim</a> extension keeps my muscle memory intact. Same motions, same leader groups, same surround and comment bindings. VS Code handles what it&rsquo;s good at (LSP UI, extensions, integrated terminal), Neovim handles the editing.</p>
<h2 id="visual-consistency">Visual consistency</h2>
<p><a href="https://github.com/catppuccin/catppuccin" target="_blank" rel="noopener noreffer ">Catppuccin Mocha</a> on everything. Terminal, editor, status bar, input method, file manager, system monitor. <a href="https://github.com/tonsky/FiraCode" target="_blank" rel="noopener noreffer ">Fira Code</a> for code with ligatures, <a href="https://ia.net/topics/a-typographic-christmas" target="_blank" rel="noopener noreffer ">iA Writer Duo S</a> for prose in Obsidian, <a href="https://github.com/adobe-fonts/source-han-sans" target="_blank" rel="noopener noreffer ">Source Han Sans</a> for Chinese fallback. When the tool changes and the colors don&rsquo;t, context switches cost less attention.</p>
<h2 id="bilingual-by-design">Bilingual by design</h2>
<p>Chinese input is built into the config, not bolted on. <a href="https://rime.im/" target="_blank" rel="noopener noreffer ">Rime</a> runs on both macOS and Windows with the same customizations, using <a href="https://github.com/iDvel/rime-ice" target="_blank" rel="noopener noreffer ">rime-ice</a> as the base schema. The editor auto-switches to English when leaving insert mode via <a href="https://github.com/keaising/im-select.nvim" target="_blank" rel="noopener noreffer ">im-select</a>, so typing Chinese never interferes with vim motions. CJK/ASCII spacing is handled automatically on save by <a href="https://github.com/hotoo/pangu.vim" target="_blank" rel="noopener noreffer ">pangu.vim</a>. I write in two languages daily, so the tooling has to treat both as first-class.</p>
<h2 id="notes-live-in-the-editor">Notes live in the editor</h2>
<p><a href="https://obsidian.md/" target="_blank" rel="noopener noreffer ">Obsidian</a> is where I think. Notes, references, daily journals, project planning. The editor config connects to my vault directly via <a href="https://github.com/obsidian-nvim/obsidian.nvim" target="_blank" rel="noopener noreffer ">obsidian.nvim</a>, so I can search notes, follow wiki-links, and open daily notes without leaving Neovim. When the vault path is set in <code>machine.json</code>, everything activates automatically. When it&rsquo;s not, the plugins stay out of the way.</p>
<h2 id="local-stays-local">Local stays local</h2>
<p>Machine-specific secrets and paths live in local override files that chezmoi never touches. A single <code>machine.json</code> gives every tool runtime context (vault paths, device identity, input method) without duplicating config per tool. What&rsquo;s personal stays off git. What&rsquo;s shared stays portable.</p>
<h2 id="keyboard-first">Keyboard-first</h2>
<p>Caps Lock is dual-purpose: tap for Escape, hold for Hyper. <a href="https://karabiner-elements.pqrs.org/" target="_blank" rel="noopener noreffer ">Karabiner-Elements</a> on macOS, <a href="https://github.com/jtroo/kanata" target="_blank" rel="noopener noreffer ">Kanata</a> on Windows. Vi mode in the shell, vim keybindings in the file manager, vim motions in VS Code. The mouse works for scrolling and selecting, but my hands mostly stay on home row.</p>
<h2 id="new-machine-in-minutes">New machine in minutes</h2>
<p>Install chezmoi, run init, answer a few prompts, and the platform installer handles the rest. CLI tools, fonts, plugins, runtimes. Adding a new tool means dropping a config file into the chezmoi source and optionally adding a line to the installer. The structure is simple enough that extending it doesn&rsquo;t require understanding the whole thing.</p>
<h2 id="survive-everything">Survive everything</h2>
<p>Sessions auto-save and auto-restore via <a href="https://github.com/tmux-plugins/tmux-resurrect" target="_blank" rel="noopener noreffer ">tmux-resurrect</a> and <a href="https://github.com/tmux-plugins/tmux-continuum" target="_blank" rel="noopener noreffer ">tmux-continuum</a>. Undo history persists across restarts. <a href="https://github.com/ajeetdsouza/zoxide" target="_blank" rel="noopener noreffer ">zoxide</a> remembers directories. If the terminal crashes, the tmux sessions are still there. If the machine reboots, one command rebuilds the rest from git. Nothing important lives only in memory.</p>
<hr>
]]></description></item><item><title>Notes for SwiftUI and Combine Programming (1)</title><link>https://chaoruan.xyz/en/posts/notes-for-swiftui-and-combine-programming/</link><pubDate>Fri, 08 Nov 2019 22:54:00 +1100</pubDate><author>blog@chaoruan.dev (Chao Ruan)</author><guid>https://chaoruan.xyz/en/posts/notes-for-swiftui-and-combine-programming/</guid><description><![CDATA[<p>I start reading <a href="https://onevcat.com/" target="_blank" rel="noopener noreffer ">onevcat</a> new book <a href="https://objccn.io/products/swift-ui" target="_blank" rel="noopener noreffer "><em>SwiftUI and Combine Programming</em></a> (a great
book to learning SwiftUI if you can read Chinese). I decided to put all the
interest parts and notes in here.</p>
<h2 id="first-things-first">First things First</h2>
<p>We always need to <code>import</code> the dependency first</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-swift">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-swift" data-lang="swift"><span class="line"><span class="cl"><span class="kd">import</span> <span class="nc">SwiftUI</span>
</span></span><span class="line"><span class="cl"><span class="kd">import</span> <span class="nc">Combine</span></span></span></code></pre></div></div>
<h2 id="layout-all-the-views-in-the-body">Layout all the views in the body</h2>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-swift">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-swift" data-lang="swift"><span class="line"><span class="cl"><span class="kd">var</span> <span class="nv">body</span><span class="p">:</span> <span class="n">some</span> <span class="n">View</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">/// Layout your view here</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span></span></span></code></pre></div></div>
<p><code>some View</code> is a new concept introduced in Swift 5.1, which called <a href="https://docs.swift.org/swift-book/LanguageGuide/OpaqueTypes.html" target="_blank" rel="noopener noreffer ">Opaque
Types</a>. Maybe someday I will write a more detail post for opaque types, in
short, opaque types kind like <code>protocol</code> but more powerful.</p>
<h2 id="enable-canvas-in-xcode">Enable Canvas in Xcode</h2>
<p>You can preview the UI layouts using <em>Canvas</em>, which is convincing by
<code>PreviewProvider</code>. As long as your swift file have a <code>struct</code> confirm it, you
will able to work with SwiftUI and preview changes using <em>Canvas</em> support (Xcode 11+ and OS X
10.15 + Only)</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-swift">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-swift" data-lang="swift"><span class="line"><span class="cl"><span class="kd">struct</span> <span class="nc">MyView_Previews</span><span class="p">:</span> <span class="n">PreviewProvider</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">static</span> <span class="kd">var</span> <span class="nv">previews</span><span class="p">:</span> <span class="n">some</span> <span class="n">View</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">MyView</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span></span></span></code></pre></div></div>
<h3 id="canvas-is-not-fast">Canvas is not fast</h3>
<p><strong>At least not fast as I wish</strong>, we still need to build the whole project
first, then it starts working as we hope. But sometimes I found I may
easier break the UI, then Xcode start not happy anymore, showing this on canvas</p>
<figure>
</figure>

<p>In this case, we have to <strong>resume</strong> the canvas which <strong>rebuilds</strong> the project again 😢</p>
<h3 id="preview-with-multiple-devices">Preview with multiple devices</h3>
<p>Canvas support preview with all devices, so we can work on different size
of screens:</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-swift">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-swift" data-lang="swift"><span class="line"><span class="cl"><span class="kd">struct</span> <span class="nc">ContentView_Previews</span> <span class="p">:</span> <span class="n">PreviewProvider</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">static</span> <span class="kd">var</span> <span class="nv">previews</span><span class="p">:</span> <span class="n">some</span> <span class="n">View</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Group</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">ContentView</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">            <span class="n">ContentView</span><span class="p">().</span><span class="n">previewDevice</span><span class="p">(</span><span class="s">&#34;iPhone SE&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="n">ContentView</span><span class="p">().</span><span class="n">previewDevice</span><span class="p">(</span><span class="s">&#34;iPad Air 2&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span></span></span></code></pre></div></div>
<p>After some loading 😫, we should see something like this:</p>
<figure>
</figure>]]></description></item><item><title>Running Oh-My-Zsh inside Spacemacs</title><link>https://chaoruan.xyz/en/posts/running-oh-my-zsh-inside-spacemacs/</link><pubDate>Sat, 26 Oct 2019 10:58:00 +1100</pubDate><author>blog@chaoruan.dev (Chao Ruan)</author><guid>https://chaoruan.xyz/en/posts/running-oh-my-zsh-inside-spacemacs/</guid><description><![CDATA[<p>When I start using <a href="http://spacemacs.org/" target="_blank" rel="noopener noreffer ">Spacemacs</a>, I was hoping there is a way to using my own configured <a href="https://ohmyz.sh/" target="_blank" rel="noopener noreffer ">zsh</a>
inside the Spacemacs workflow, after a look through the documentation, of
course we can do it</p>
<p>First, we need to let Spacemacs load zsh when it setup <code>shell</code>, and I found
you can do a pop-up buffer style with 30% of the current height from the
bottom.</p>
<p>So in <code>.spacemacs</code> we can set this in <code>dotspacemacs-configure-layers</code>:</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-emacs-lisp">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nv">shell</span> <span class="nb">:variables</span>
</span></span><span class="line"><span class="cl">       <span class="nv">shell-default-term-shell</span> <span class="s">&#34;/bin/zsh&#34;</span> <span class="c1">;; find your zsh path using `$ whereis zsh`</span>
</span></span><span class="line"><span class="cl">       <span class="nv">shell-default-height</span> <span class="mi">30</span>
</span></span><span class="line"><span class="cl">       <span class="nv">shell-default-position</span> <span class="ss">&#39;bottom</span><span class="p">)</span></span></span></code></pre></div></div>
<p>This change is the basic setup, <strong>but since I enabled <code>vi</code> key bindings in my
zsh, it starts conflicts with Spacemacs <a href="https://github.com/emacs-evil/evil" target="_blank" rel="noopener noreffer ">evil-mode</a>.</strong> after a play around with different
settings, I found the best option for me is to disable the evil-mode inside
<a href="https://www.emacswiki.org/emacs/AnsiTerm" target="_blank" rel="noopener noreffer ">ansi-term</a>.</p>
<p>Add following code in <code>dotspacemacs/user-config</code>:</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-emacs-lisp">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nv">evil-set-initial-state</span> <span class="ss">&#39;term-mode</span> <span class="ss">&#39;emacs</span><span class="p">)</span></span></span></code></pre></div></div>
<p>This change allows us navigation in ansi-term, but we can not editing
anything in the input line. We need to do <strong>one more change</strong>:</p>
<div class="code-block code-line-numbers open" style="counter-reset: code-block 0">
    <div class="code-header language-emacs-lisp">
        <span class="code-title"><i class="arrow fas fa-angle-right" aria-hidden="true"></i></span>
        <span class="ellipses"><i class="fas fa-ellipsis-h" aria-hidden="true"></i></span>
        <span class="copy" title="Copy to clipboard"><i class="far fa-copy" aria-hidden="true"></i></span>
    </div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-emacs-lisp" data-lang="emacs-lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nv">evil-set-initial-state</span> <span class="ss">&#39;term-mode</span> <span class="ss">&#39;emacs</span><span class="p">)</span> <span class="c1">;; turn off evil-mode for ansi-term</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nb">setq</span> <span class="nv">term-char-mode-point-at-process-mark</span> <span class="no">nil</span><span class="p">)</span> <span class="c1">;; allow editing in normal mode</span></span></span></code></pre></div></div>
<p>After <code>SPC f e R</code>, we can now using zsh inside Spacemacs</p>
<h2 id="reference">Reference</h2>
<ul>
<li><a href="https://github.com/syl20bnr/spacemacs/issues/8642" target="_blank" rel="noopener noreffer ">syl20bnr/spacemacs#8642 Cannot edit shell commands in normal mode.</a></li>
<li><a href="https://github.com/syl20bnr/spacemacs/tree/develop/layers/&#43;tools/shell" target="_blank" rel="noopener noreffer ">spacemacs/layers/+tools/shell at develop · syl20bnr/spacemacs · GitHub</a></li>
</ul>]]></description></item></channel></rss>